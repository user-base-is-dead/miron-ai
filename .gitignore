# Virtual Environments
Miron/
Miron311/
# existing on-disk venv (rename/recreate as Miron311, then delete this line)
Miron311/
venv/
.venv/

# Python cache
__pycache__/
*.pyc
*.pyo

# Data files (too large for GitHub - use the data-download/ scripts)
data/

# Keep saved_model but ignore large checkpoint files
saved_model/*.pt
saved_model/*.pth
saved_model/*.bin
# (config.json will still be tracked)

# Tokenizer self-test output + transient smoke-test artifacts
tokenizer_config.json
_smoke_out/

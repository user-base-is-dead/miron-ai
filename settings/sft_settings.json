{
  "max_steps": 2000,
  "lr": 2e-5,
  "min_lr": 2e-6,
  "warmup_steps": 50,
  "eval_every": 200,
  "eval_iters": 50,
  "log_every": 10,
  "save_every": 200,
  "batch_size": 1,
  "grad_accum": 64,
  "optimizer_type": "paged_adamw_8bit",
  "num_workers": 2
}
